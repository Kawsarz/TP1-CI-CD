name: CI (dev)

on:
  push:
    branches: [ "dev" ]        # run only when pushing to dev
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:            # allow manual run

jobs:
  unittest:                     # first (and only) stage for now
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 👇 Replace this block with your real test commands
      - name: Run unit tests (placeholder)
        run: |
          echo "Running unit tests..."
          sleep 5
          echo "Tests complete ✅"
